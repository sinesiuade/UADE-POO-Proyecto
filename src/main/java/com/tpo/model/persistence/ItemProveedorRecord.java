package com.tpo.model.persistence;

/** Registro plano (JSON) de un ítem provisto por un proveedor, con su precio acordado. */
public class ItemProveedorRecord {
    public int codigoItem;   // código del Item del catálogo
    public double precio;    // precio que ofrece el proveedor por ese ítem
}
